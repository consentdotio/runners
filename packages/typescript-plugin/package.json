{
  "name": "@runners/typescript-plugin",
  "version": "0.0.0",
  "description": "TypeScript Language Service Plugin for Runners SDK",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "tsc --build --clean && rm -r dist ||:",
    "test": "vitest run src",
    "test:watch": "vitest watch src",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "typescript",
    "language-service-plugin",
    "runners",
    "playwright"
  ],
  "peerDependencies": {
    "typescript": ">=5.0.0"
  },
  "devDependencies": {
    "@runners/tsconfig": "workspace:*",
    "@types/node": "^22.10.2",
    "typescript": "^5.9.3",
    "vitest": "^2.1.8"
  }
}

