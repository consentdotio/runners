// Re-export config functionality from @runners/config
// Note: This creates a re-export, but the actual implementation is in @runners/config
export { defineConfig, type RunnersConfig } from "@runners/config";
